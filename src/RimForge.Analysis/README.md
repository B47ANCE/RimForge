# RimForge.Analysis

The analysis platform converts discovered `ModRecord` values into queryable dependency, ordering, diagnostic, and impact information.

The project must remain independent of WPF, file-system discovery, persistence, and PowerShell execution.
