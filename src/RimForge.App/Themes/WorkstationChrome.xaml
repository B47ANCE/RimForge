<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Pass 41 workstation-chrome semantic resources. Keep behavior out of this dictionary. -->
  <SolidColorBrush x:Key="WorkstationChromeBackgroundBrush" Color="{DynamicResource Bg0}"/>
  <SolidColorBrush x:Key="WorkstationChromeRaisedBrush" Color="{DynamicResource Bg1}"/>
  <SolidColorBrush x:Key="WorkstationChromeHoverBrush" Color="{DynamicResource Bg3}"/>
  <SolidColorBrush x:Key="WorkstationChromePressedBrush" Color="{DynamicResource PressedSurface}"/>
  <SolidColorBrush x:Key="WorkstationChromeBorderBrush" Color="{DynamicResource Bg4}"/>
  <SolidColorBrush x:Key="WorkstationChromeQuietBorderBrush" Color="#35383E"/>
  <SolidColorBrush x:Key="WorkstationChromeDisabledBrush" Color="{DynamicResource Disabled}"/>
  <SolidColorBrush x:Key="WorkstationChromeSearchBrush" Color="#18191C"/>
  <SolidColorBrush x:Key="WorkstationChromeStatusBrush" Color="#191A1D"/>

  <CornerRadius x:Key="WorkstationChromeCornerRadius">7</CornerRadius>
  <Thickness x:Key="WorkstationChromeControlPadding">10,7</Thickness>

  <Style x:Key="WorkstationChromeButton" TargetType="Button">
    <Setter Property="Height" Value="38"/>
    <Setter Property="MinWidth" Value="42"/>
    <Setter Property="Padding" Value="10,0"/>
    <Setter Property="Margin" Value="0"/>
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Surface"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{DynamicResource WorkstationChromeCornerRadius}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Surface" Property="Background" Value="{DynamicResource WorkstationChromeHoverBrush}"/>
              <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource WorkstationChromeBorderBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Surface" Property="Background" Value="{DynamicResource WorkstationChromePressedBrush}"/>
              <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Surface" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource WorkstationChromeDisabledBrush}"/>
              <Setter Property="Cursor" Value="Arrow"/>
              <Setter TargetName="Surface" Property="Opacity" Value="0.52"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="WorkstationChromeGlyph" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets"/>
    <Setter Property="FontSize" Value="15"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
    <Setter Property="HorizontalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="WorkstationChromeActionLabel" TargetType="TextBlock">
    <Setter Property="FontSize" Value="11"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Margin" Value="7,0,0,0"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="WorkstationChromeSeparator" TargetType="Border">
    <Setter Property="Width" Value="1"/>
    <Setter Property="Height" Value="26"/>
    <Setter Property="Margin" Value="9,0"/>
    <Setter Property="Background" Value="{DynamicResource WorkstationChromeQuietBorderBrush}"/>
    <Setter Property="HorizontalAlignment" Value="Center"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="WorkstationChromeStatusSurface" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource WorkstationChromeStatusBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource WorkstationChromeQuietBorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="7"/>
    <Setter Property="Padding" Value="11,6"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <Style x:Key="WorkstationChromeSearchSurface" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource WorkstationChromeSearchBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource WorkstationChromeQuietBorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="8"/>
    <Setter Property="Padding" Value="1"/>
  </Style>
</ResourceDictionary>
